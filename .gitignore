# --- Secrets: never commit ---
.env
.env.*
*.key
*.pem
credentials*
*-secret*
*-secrets*

# --- Local dev tooling ---
.claude/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
desktop.ini

# --- Design-export tooling / internal artifacts (not part of the served site) ---
.thumbnail
_adherence.oxlintrc.json
_ds_manifest.json
SKILL.md
guidelines/
templates/
uploads/

# Stale source copies of the pages — the live versions now live at repo root.
ui_kits/
